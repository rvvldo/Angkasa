export const MAINTENANCE_CONFIG = {
  user: false,        // Untuk standard users (Forum, Profile, dll.)
  dashAdmin: false,   // Untuk DashAdmin (/DashAdmin)
  adminCentral: false // Untuk Admin Central (/admin/central)
};